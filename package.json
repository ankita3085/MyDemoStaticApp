{
  "name": "mydemostaticapp-resume-parser",
  "version": "1.0.0",
  "private": true,
  "description": "Resume parser helper for Ankita profile site",
  "scripts": {
    "parse-resume": "node scripts/parseResume.js",
    "generate-resume": "node scripts/generateResumeHtml.js",
    "build": "npm run generate-resume && node scripts/buildStatic.js",
    "build:azure": "npm run build"
  },
  "dependencies": {
    "mammoth": "^1.4.19"
  }
}
